# spore-engine-vue2

vue2 ssr h5 页面渲染引擎

## 特性

## 约定

默认 `src/pages` 下存放项目入口文件。

入口文件路径信息以 `process.env.PAGES_DIR` 环境变量形式传递给开发与构建服务。

`src/hook.js` 为服务初始化配置注入文件。

## 开发

```bash
npm run dev
```

## 构建

```bash
npm run build
```

## 执行环境
